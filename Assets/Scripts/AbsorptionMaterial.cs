using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AbsorptionMaterial : MonoBehaviour
{
    public float diffAbsorption = 0.2f;
    public float specAbsorption = 0.4f;
    public float diffuseProportion = 0.3f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
